let x = 10;
let y = 20;
let z = 20;

// frage : sind x und y und das gleiche?

let xGleichy = (x=== y);
let yGleichz = (y === z);

console.log(x>y);

// x < y ist x kleiner als y
// x > y ist x grösser als y
// x <= y ist x kleiner oder gleich y
// x === y ist x das gleiche wie y
// x !== y ist x ist nicht gleich wie y
// x * y Multiplizierer
// x / y Sutrahieren
// x % y ist der rest einer teilung


// frage ? wenn die andword true ist : wenn die andword ist fals
let z = (x === y) ? 3 : 10; // 10
let andword = ( x === y) ? "x ist y" : "x ist nicht y";
console.log (andword);

let wort1 = "hallo";
let wort2 = " welt";
let stringVegleich = (wort1 === wort2) ? "gleich" : "ungleich";
console.log (stringVegleich); // ungleich

